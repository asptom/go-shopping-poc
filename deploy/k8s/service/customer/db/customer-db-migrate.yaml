apiVersion: batch/v1
kind: Job
metadata:
  name: customer-db-migrate
  namespace: shopping
spec:
  ttlSecondsAfterFinished: 300
  backoffLimit: 0
  template:
    spec:
      initContainers:
        - name: wait-for-db
          image: docker.io/postgres:18.0
          envFrom:
            - secretRef:
                name: postgres-admin-bootstrap-secret
            - secretRef:
                name: customer-db-secret
          command: ["/bin/sh", "-c"]
          args:
            - |
              export PGPASSWORD="$POSTGRES_PASSWORD"
              until pg_isready -h "$POSTGRES_HOST" -p 5432 -U "$POSTGRES_USER" -d "$db_name" >/dev/null 2>&1; do
                echo "Waiting for database to be ready..."
                sleep 5
              done
              echo "Database ${db_name} is ready."
      restartPolicy: OnFailure
      containers:
        - name: migrate
          image: docker.io/postgres:18.0
          env:
            - name: PGHOST
              valueFrom:
                secretKeyRef:
                  name: postgres-admin-bootstrap-secret
                  key: POSTGRES_HOST
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: customer-db-secret
                  key: username
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: customer-db-secret
                  key: password
            - name: PGDATABASE
              valueFrom:
                secretKeyRef:
                  name: customer-db-secret
                  key: db_name
          volumeMounts:
            - name: migrations
              mountPath: /migrations
          command:
            - bash
            - -c
            - |
              for f in /migrations/*.sql; do
                psql -f "$f"
              done
      volumes:
        - name: migrations
          configMap:
            name: customer-db-migrations-sql
