Our goal is to create a plan to build out a service that will be used to create and manage orders for our POC shopping application.  There is some code in the codebase already that will "belong" to the order process: ./internal/contracts/events/order.go and ./internal/service/order/migrations/001-init.sql. Critically evaluatee the DDL defined in 001-init.sql and propose any needed changes to ensure the best possible data structure for orders.  Databases and database password configmaps are created using the services.mk file - the only thing to be done there is to add order to the SERVICES variable, add SERVICE_BUILDS_DB_order := true, and add SERVICE_INTERNAL_DIR_order := order in services.mk

The order service should expose api endpoints that allow a front-end application to GET a single order, GET all orders for a given user, cancel an order (if it has not already been shipped/closed/cancelled).  The order service will subscribe to the cart.checked_out event, create an order from the items in the associated cart when it receives that event, and then emit an order.created event.  The customer and eventreader services provide excellent examples of how the service should be coded.  The deployment files in deploy/k8s/cart and customer provide good examples of the structure and files needed to correctly set up what is needed to build a database, to deploy the service using a statefulset (with environment variables), and to create the configmap for service specific variables (shared platform variables will come from the platform configmap so no change will be needed there).  Be sure to create a Dockerfile in /cmd/order so that the service can be deployed (using the examples from customer or cart)

Create a detailed plan that can be used by another LLM to implement the plan exactly as intended.  The plan should use phases that are in the optimal operational order.  This is a new service, so there are no breaking changes - no backwards compatibility is necessary.  Critically evaluate each step of the plan to ensure it is using the best and most idiomatic approach.  As stated above, there are excellent examples to use as this service is created - use those examples and do not create new architectural patterns.

I have placed you in build mode - create a file called docs/order_service_implementation_plan.md and write the plan to that file.  You can read the codebase, but make no changes to any other files within the project.

Ask clarifying questions if needed,  Provide me with an indication that you have read and understood AGENTS.md and .go-docs/00-LLMrules.md
